{
    "compilerOptions": {
        "target": "es6",
        // The generated files go here by default, unless --genDir is set.
        "outDir": "built"
    },

    "extensions": {
       "tsetse": {"fallthrough": true}
    }
}
